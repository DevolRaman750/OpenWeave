# OpenWeave core runtime — production dependencies.
# Pinned to lower bounds verified against Python 3.12.
#
# Install:  pip install -r requirements.txt
# (For the research/plotting extras under adaptive_baseline, also install
#  scipy and matplotlib — not required by the sidecar or observer.)

fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
numpy>=1.26,<3.0

# External judgement / embedding clients (used by deep_evaluation + cycle_detection).
# Network calls are bounded by timeout/retry/circuit-breaker in openweave_core.resilience.
anthropic>=0.40,<1.0
openai>=1.40,<2.0
